name: 06_webmail_exfiltration
tier: synthetic
category: insider_threat
difficulty: intermediate
description: >
  Webmail-based data exfiltration. Browser history shows Gmail
  compose/send activity correlated with access to sensitive documents.
  Tests browser_history parser integration with the detection pipeline.

source:
  provider: synthetic
  license: internal

fixtures:
  mft: mft.csv
  prefetch: prefetch.csv
  evtx: evtx.csv
  browser_history: browser_history.csv

expected:
  malicious_executables: []
  finding_counts:
    webmail_exfiltration: 1
    total: 1
  min_precision: 1.0
  min_recall: 1.0

notes: |
  Related MITRE ATT&CK technique: T1567 (Cloud exfiltration via webmail).
  Requires browser_history parser. Not yet wired into scenario_harness.py.
